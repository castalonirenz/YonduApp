import React, { Component } from 'react';
import { View, Text } from 'react-native';

class CSecurity extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> CSecurity </Text>
      </View>
    );
  }
}

export default CSecurity;
