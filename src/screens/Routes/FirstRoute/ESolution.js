import React, { Component } from 'react';
import { View, Text } from 'react-native';

class ESolution extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> ESolution </Text>
      </View>
    );
  }
}

export default ESolution;
